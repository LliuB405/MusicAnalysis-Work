# 网易云音乐热歌榜数据分析 - 依赖清单
# Python >= 3.8

# === 运行时依赖 ===
flask>=2.0.0
requests>=2.28.0
beautifulsoup4>=4.11.0
matplotlib>=3.5.0
wordcloud>=1.8.0
pandas>=1.4.0
openpyxl>=3.0.0    # Excel 导出
reportlab>=4.0.0   # PDF 报表

# 定时刷新由 src/music_analytics/scheduler.py 的轻量后台线程实现，
# 不依赖 APScheduler。

# === 开发依赖 ===
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0            # 代码格式化
mypy>=0.950              # 类型检查
ruff>=0.1.0              # 替代 flake8 + isort 的快速 linter
pre-commit>=3.0.0        # Git 钩子管理
