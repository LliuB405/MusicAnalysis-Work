# Public Spotify application identifier from https://developer.spotify.com/dashboard
SPOTIFY_CLIENT_ID=your_spotify_client_id

# Add this exact URL to the app's Redirect URIs in the Spotify dashboard.
SPOTIFY_REDIRECT_URI=http://127.0.0.1:5000/player

# No Spotify Client Secret is needed or accepted: the browser uses OAuth PKCE.
